import { StyleSheet } from 'react-native';

export const Spacing = StyleSheet.create({
  centered: {
    flex: 1, 
    justifyContent: "center", 
    alignItems: "center", 
  },
  separate: {
    margin: 10,
    padding: "5%"
  }
});